---
title: "Booking flow"
description: "Build a booking flow with catalog, availability, price, and booking endpoints."
icon: "calendar-check"
---

Use catalog endpoints to build selection screens. Then validate availability and price before you create a booking.

For exact parameters and response schemas, use the [API reference](/api).

## Recommended flow

1. Fetch destinations with `GET /destinations`.
2. Fetch hotels with `GET /hotels`.
3. Fetch visit types with `GET /visit-types`.
4. Fetch room availability with `GET /calendar/dates`.
5. Fetch possible checkout dates with `GET /calendar/checkouts`.
6. Fetch the final price with `GET /price`.
7. Create a booking with `POST /booking`.

<Card title="API v1 endpoints" icon="square-terminal" href="/api/v1">
  Browse the current production endpoint reference.
</Card>

## Opening the calendar

On the first calendar load, call `GET /calendar/dates` with a hotel, room type, and visit type but without `date_from` and `date_to`. The response starts at the first month that contains a stay you can actually book — one where every night of the minimum stay has both remaining availability and a price — instead of at the current month.

That way you do not have to page through empty months to find the first date a guest can pick. Once the guest navigates the calendar, send `date_from` and `date_to` for the month you want; explicit dates are never moved.

## Booking configuration

Use `GET /booking` when you need SpaPortal to match a partial selection by names.

For example, you can pass a destination name, hotel name, visit type name, room type category, and date range. The response returns matching objects, availability, and either a final price or lowest price.

## Binding bookings

When `binding_booking` is `true`, the billed contact must include an address.

Use `binding_booking: false` for non-binding booking requests.
