---
title: "Webhooks"
description: "Webhook status for SpaPortal public API integrations."
icon: "webhook"
---

There is no public webhook contract documented for the current API v1 implementation.

For now, build integrations around synchronous API calls.

When SpaPortal exposes public webhook events, document each event here with:

- Event name
- Delivery URL setup
- Signature verification
- Retry behavior
- Example payload
