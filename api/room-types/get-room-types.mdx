---
title: "Get room types"
description: "Returns room types available to your distribution channel."
---

```http
GET /api/v2/room-types
```

Returns room types available to your distribution channel.

## Query parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `limit` | integer | Number of records to return. Default is `25`. Maximum is `100`. |
| `cursor` | string | Cursor from the previous page. |
| `hotelId` | string | Filter by hotel ObjectId. |
| `destinationId` | string | Filter by destination ObjectId. |
| `visitTypeId` | string | Filter by visit type ObjectId. |
| `beds` | integer | Filter by number of beds. |
| `code` | string | Filter by room type code. |
| `search` | string | Search by localized category name. |
| `dateFrom` | date | Start date for availability and price context. |
| `dateTo` | date | End date. Must be after `dateFrom`. |
| `availableOnly` | boolean | Return only room types with availability in the supplied date range. Requires `dateFrom` and `dateTo`. |
| `currency` | string | Comma-separated currency list, for example `EUR,CZK`. |
| `include` | string | Use `hotel` to include basic hotel data. |
| `sort` | string | One of `createdAt:desc`, `createdAt:asc`, `id:asc`, `id:desc`. |

## Example

```bash
curl \
  -H "X-SpaPortal-Public-Key: <public_key>" \
  -H "Accept-Language: cs-CZ,de-DE" \
  "https://spaportal.cz/api/v2/room-types?limit=25&currency=EUR,CZK&include=hotel"
```

## Response

Localized fields use the response locale keys requested by `Accept-Language`.
Regional tags such as `de-DE` are returned under the supported locale key `de`.

```json
{
  "data": [
    {
      "id": "66f16191f739705faa020003",
      "hotelId": "66f16191f739705faa020001",
      "code": "SUPERIOR-DBL",
      "category": {
        "cs-CZ": "Superior dvouluzkovy pokoj",
        "de": "Superior Doppelzimmer"
      },
      "beds": 2,
      "facilities": ["shower", "toilet"],
      "images": [
        {
          "url": "https://spaportal-files.s3.eu-central-1.amazonaws.com/production/room-types/superior.jpg",
          "thumbnailUrl": "https://spaportal-files.s3.eu-central-1.amazonaws.com/production/thumbnails/room-types/superior.webp"
        }
      ],
      "description": {
        "cs-CZ": "Pokoj s koupelnou.",
        "de": "Zimmer mit Bad."
      },
      "lowestPrices": {
        "CZK": {
          "amountMinor": 180000,
          "currency": "CZK",
          "currencyExponent": 2,
          "unit": "roomPerNight"
        },
        "EUR": {
          "amountMinor": 7200,
          "currency": "EUR",
          "currencyExponent": 2,
          "unit": "roomPerNight"
        }
      },
      "bookingUrl": {
        "cs-CZ": "https://booking.example/cs",
        "de": "https://booking.example/de"
      },
      "hotel": {
        "id": "66f16191f739705faa020001",
        "name": "Pawlik",
        "stars": 4,
        "destinationId": "66f16191f739705faa020010"
      }
    }
  ],
  "pagination": {
    "limit": 25,
    "nextCursor": null,
    "hasMore": false,
    "total": 1
  }
}
```
