---
title: "List room types"
description: "Returns a list of room types."
---

```http
GET /api/v2/room-types
```

## Query parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `limit` | integer | Number of records to return. Default is `25`. Maximum is `100`. |
| `cursor` | string | Cursor from the previous page. |
| `hotelId` | string | Filter by hotel id. |
| `beds` | integer | Filter by number of beds. Minimum `1`, maximum `10`. |
| `code` | string | Filter by room type code. |
| `search` | string | Full-text search of the localized category name (all languages). See [Search](/api/search). |
| `include` | string | Set to `hotel` to embed the hotel. See [Includes](/api/includes). |
| `sort` | string | One of `createdAt:desc`, `createdAt:asc`, `id:asc`, `id:desc` (default `createdAt:desc`). See [Sorting](/api/sorting). |

## Example

```bash
curl \
  -H "X-SpaPortal-Public-Key: <public_key>" \
  -H "Accept-Language: cs-CZ,de-DE" \
  "https://spaportal.cz/api/v2/room-types?limit=25&include=hotel"
```

## Response

`description` contains Markdown. `category` is plain text.

```json
{
  "data": [
    {
      "id": "66f16191f739705faa020003",
      "hotelId": "66f16191f739705faa020001",
      "code": "SUPERIOR-DBL",
      "category": {
        "cs-CZ": "Superior dvouluzkovy pokoj",
        "de": "Superior Doppelzimmer"
      },
      "beds": 2,
      "facilities": ["shower", "toilet"],
      "images": [
        {
          "url": "https://spaportal-files.s3.eu-central-1.amazonaws.com/production/room-types/superior.jpg",
          "thumbnailUrl": "https://spaportal-files.s3.eu-central-1.amazonaws.com/production/thumbnails/room-types/superior.webp"
        }
      ],
      "description": {
        "cs-CZ": "Pokoj s koupelnou.",
        "de": "Zimmer mit Bad."
      },
      "hotel": {
        "id": "66f16191f739705faa020001",
        "name": "Pawlik",
        "stars": 4,
        "destinationId": "66f16191f739705faa020010"
      }
    }
  ],
  "pagination": {
    "limit": 25,
    "nextCursor": null,
    "hasMore": false,
    "total": 1
  }
}
```
