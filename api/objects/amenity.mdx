---
title: "Amenity"
description: "An entry in the amenity dictionary."
openapi-schema: "/api/v2/openapi.yaml Amenity"
---

<Warning>
  **Draft — not yet available.** This is a work-in-progress contract. It may change
  before release; do not build against it.
</Warning>

An entry in the [amenity dictionary](/api/amenities): a stable `code`, its `category`,
the `scope` it applies to (`hotel`, `roomType`, or both), a `valueType`, and a
localized `label`.
