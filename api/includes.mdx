---
title: "Includes"
description: "Embed related resources with the include parameter."
icon: "layer-group"
---

Use the `include` query parameter to embed related resources in the response
instead of fetching them separately. Nothing is embedded by default.

```bash
GET /api/v2/room-types?include=hotel
```

Each endpoint lists the `include` values it supports and what each adds.
